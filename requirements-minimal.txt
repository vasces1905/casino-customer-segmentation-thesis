# Minimal requirements for casino-customer-segmentation
# Core dependencies only - no heavy ML libraries for basic setup

# Database
psycopg2-binary==2.9.7

# Data processing (lightweight)
pandas==2.0.3
numpy==1.24.3

# Basic ML (will download models separately)
scikit-learn==1.3.0

# Configuration
python-dotenv==1.0.0

# Logging
loguru==0.7.0

# Optional: For model loading (if needed)
joblib==1.3.2
